{
    "preset": "laravel",
    "exclude": [
        "scripts"
    ],
    "rules": {
        "not_operator_with_successor_space": true,
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    }
}
